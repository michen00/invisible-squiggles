# Test Workspace

This folder is used by the VS Code test runner (`@vscode/test-cli`) as the workspace opened during
integration and E2E tests.

## Documentation

Dummy section for testing README pre-build processing
